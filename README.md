# JavascriptYou have to create one custom plugin in which you need to create a setting and widget page on WordPress backend as attached image (setting_widget_page.png).
- You need to save all settings using register_setting.
- Create a widget and widget field is same as per attached image (widget_background.png)
- It's output is display as per attached image (widget_frontend.png)
- If there is error on form submit then form data should not be lost. All fields value should remain as it is.
- Server side validation for any two fields .
- Try to use the same structure as an attachment. 
